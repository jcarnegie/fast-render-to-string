var render<%= componentName %> = function() {
    var children = [];
    this.props = arguments[0] || {};
    for(var i = 1; i < arguments.length; i++)
        children[i] = arguments[i];
    <%= renderBody %>
}